pipeline {
    agent any
    stages {
        stage('Uche'){
            steps {
                sh 'ps -ef'
                sh 'sudo systemctl status uche'
            }
        }
    }
}
